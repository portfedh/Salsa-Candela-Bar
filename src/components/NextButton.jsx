function NextButton() {
  return (
    <button
      style={{
        color: "white",
        backgroundColor: "#E04A25",
        border: "none",
        padding: ".5rem 5.5rem",
        fontSize: "1rem",
        fontWeight: "700",
        borderRadius: "20px",
      }}
    >
      Siguiente
    </button>
  );
}

export default NextButton;
